# Cambia el dominio público del backend al preparar la app para producción.
# Para Android local con ADB usa http://127.0.0.1:3200 y `adb reverse tcp:3200 tcp:3200`.
EXPO_PUBLIC_API_URL=https://api.tu-dominio.com
